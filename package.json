{
  "name": "structo",
  "version": "1.0.0",
  "description": "Structo Engineering Suite Mobile App",
  "main": "app.js",
  "scripts": {
    "build": "mkdir -p www && cp index.html styles.css manifest.json service-worker.js sw-register.js icon.svg *.png www/ && npx javascript-obfuscator app.js --output www/app.js",
    "add-android": "npx cap add android",
    "sync": "npx cap sync",
    "generate-icons": "npx @capacitor/assets generate --android --iconBackgroundColor '#121212'",
    "build-apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^6.3.1",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1",
    "javascript-obfuscator": "^4.1.0"
  }
}
